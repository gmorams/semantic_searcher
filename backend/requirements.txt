# Backend HTTP layer (lo que el contenedor api necesita por encima del dominio)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.6.0
python-multipart>=0.0.9

# El backend reusa el dominio existente, asi que arrastra sus dependencias:
-r ../requirements.txt

# Tests
pytest>=8.0.0
httpx>=0.27.0
